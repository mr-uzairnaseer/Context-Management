<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/layout_swipe_to_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <de.komoot.android.widget.KmtRecyclerView
            android:id="@+id/inspiration_feed_rv"
            android:background="@color/canvas"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:listSelector="@android:color/transparent"
            android:cacheColorHint="@android:color/transparent"
            android:divider="@color/transparent"
            android:dividerHeight="0px"
            android:headerDividersEnabled="false"
            android:footerDividersEnabled="false"/>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
    <ImageView
        android:id="@+id/imageview_bottom_decoration"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:layout_alignParentBottom="true"/>
    <Button
        android:textSize="14sp"
        android:textColor="@color/white"
        android:layout_gravity="top|center_horizontal"
        android:id="@+id/textview_floating_new_header"
        android:background="@drawable/btn_green_new_available_states"
        android:paddingLeft="20dp"
        android:paddingTop="0dp"
        android:paddingRight="20dp"
        android:paddingBottom="0dp"
        android:layout_width="wrap_content"
        android:layout_height="22dp"
        android:text="@string/isshv_floating_new_header"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:fontFamily="?attr/fontBold"/>
</RelativeLayout>
